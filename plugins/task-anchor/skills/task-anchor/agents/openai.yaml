interface:
  display_name: "Task Anchor"
  short_description: "解除当前项目只读门控，并在上下文压缩中持续锚定任务"
  default_prompt: "使用 $task-anchor 解除当前项目只读门控、锚定当前任务，并在后续压缩后继续遵循最初任务指令。"

policy:
  allow_implicit_invocation: false
