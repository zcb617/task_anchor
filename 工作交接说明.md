# Task Anchor 工作交接说明

> 用途：把本文档作为附件交给新的 Codex 远程会话。接手会话应先完整阅读本文，再按“建议的首条锚定指令”继续；不得把本文档中的历史讨论误认为需要重新实现的功能。

## 一、唯一目标

需要解决的不是通用“项目记忆”，也不是让模型每轮都读 `AGENTS.md`：

> 用户显式调用 `$task-anchor` 后，同一 Codex 任务无论发生多少次手动或自动上下文压缩，压缩完成后都必须立即重新注入**第一次调用时的原始任务指令**，使模型继续聚焦既定目标与硬约束；不得依赖用户再发送一条消息触发恢复。

设计要求：

- 每次都读取同一份原文，不能用上一次摘要再生成下一次指令。
- 注入内容要小，只包含继续使用 Skill 的要求和原始任务指令，不注入整份项目文档、历史消息、工具记录或递归摘要。
- 不以 `AGENTS.md`、`compact_prompt`、`SessionStart(source=compact)`、`PreCompact` 或“每轮重新读大文档”替代这个目标。
- 不处理无关项目；不要把讨论引向 ERP 业务代码、普通 Git 操作或泛化的知识库方案。

## 二、当前插件实现（仓库事实）

工作目录：`D:\work\task_anchor`

当前关键文件：

- `plugins/task-anchor/hooks/hooks.json`
- `plugins/task-anchor/scripts/hook_entry.py`
- `plugins/task-anchor/tests/test_hook_entry.py`
- `plugins/task-anchor/skills/task-anchor/SKILL.md`

当前代码已经使用这两个 Hook：

1. `UserPromptSubmit`
   - 只在提示词含显式 `$task-anchor` 时保存完整原始提示词。
   - 以 `session_id` 隔离任务；首次保存后不允许同一任务覆盖。
   - 同时保存项目身份哈希，防止把一个项目的锚点注入另一个项目。

2. `PostCompact`
   - 只接受 `trigger=auto` 或 `trigger=manual`。
   - 读取并校验原始指令的 SHA-256。
   - 返回 `hookSpecificOutput.hookEventName = "PostCompact"` 和 `additionalContext`。
   - `additionalContext` 内容是：要求使用 `$task-anchor` Skill 继续任务，以及原始任务指令全文。

审计日志位于 `PLUGIN_DATA/audit/events.jsonl`，不记录任务正文。一次成功恢复应依次出现：

```text
post_compact_received
anchor_loaded
restore_emitted
```

已有单元测试覆盖：首次保存、重复调用不覆盖、手动/自动 PostCompact、跨项目拒绝、损坏拒绝、会话隔离、1000 次压缩返回同一原始指令和命令入口。可运行：

```powershell
python -m unittest plugins/task-anchor/tests/test_hook_entry.py
```

### 文档与代码的版本差异

根目录的 `README.md` 与 `TASK_ANCHOR_完整方案.md` 仍有旧的 `SessionStart(source=compact)` / `0.144.1` 描述；它们不是当前 Hook 实现的真值。当前真值是上述 `hooks.json`、`hook_entry.py`、测试与 Skill：**使用 `PostCompact`，不同时注册 SessionStart 恢复。**

## 三、自定义 Codex CLI 与桌面端现状

用户已在本机源码 `D:\work\my_codex` 中实现 `PostCompact additionalContext` 支持；相关设计文档：

`https://github.com/zcb617/codex/blob/dev/from-rust-v0.144.4/codex-rs/hooks/POST_COMPACT_ADDITIONAL_CONTEXT.md`

运行环境事实：

- 桌面端本机使用自己的 bundled CLI；不要把它误认为用户的自定义 CLI。
- SSH 别名 `local` 指向本机 `127.0.0.1`，登录 Shell 是 Git Bash。
- SSH `local` 中的 `codex` 解析到 `C:\Users\zhang\.local\bin\codex.exe`，已观察到版本为 `0.144.4`；相邻的 `codex-code-mode-host.exe` 也已放置。
- 因此，桌面端要使用自定义 CLI 时，应在桌面端中选择 SSH `local` 作为运行位置；不要把“普通桌面端本机任务”当成使用了自定义 CLI。
- 直接在 `local` 新建远程聊天已能正常对话，说明 SSH 登录、Git Bash、远程 CLI 探测和基本 app-server 启动链路可用。

版本差异需要保留为验证变量：此前观察到桌面端缓存的 bundled CLI 为 `0.144.2`，SSH `local` 的自定义 CLI 为 `0.144.4`。尚无“协议版本不兼容”的直接证据，不能把它断言为故障根因。

## 四、已排除与当前阻塞

### Git 已就绪

- 本仓库已有提交，当前分支为 `master`。
- 已推送到：`git@github.com:zcb617/task_anchor.git`
- 本地 `master` 跟踪 `origin/master`。
- 源端和 SSH `local` 端均能识别同一个 `D:\work\task_anchor` Git 仓库。

### 不要再尝试把旧桌面任务 Hand off 到 `local`

桌面端 Hand off 的进度第 2 步失败。根据已安装桌面端的实现，该步骤是：

```text
第 1 步：Preparing files for transfer
第 2 步：Copying files to the destination host   ← 失败点
第 3 步：Creating/reusing a worktree              ← 尚未到达
```

第 2 步传输的是旧任务的迁移产物（包括任务 rollout 记录），不是 Git worktree 创建。因此：

- 新建远程聊天能成功，并不代表旧聊天能够迁移。
- 当前旧桌面任务的聊天记录不会自动带入新远程聊天。
- 故障窗口中桌面日志记录了 SSH `local` 的 app-server WebSocket 多次以 `1006` 异常关闭，随后自动重连；这与第 2 步传输失败一致。
- 目前没有可读的详细弹窗错误文本，不能再把根因细化到具体文件读写或版本兼容性。

这条 Hand off 路径不是当前验证目标。除非用户明确要求排查它，否则不要修改 SSH、Git、桌面端安装目录或自定义 CLI 来追它。

## 五、建议的新远程会话操作

1. 在 Codex 桌面端选择 SSH `local`，并在 `task_anchor` 项目中**新建**远程任务。
2. 将本文件作为附件提供给新任务。
3. 确认 Task Anchor 插件已安装、启用，且两个 Hook 都已允许并信任。仅安装插件不等于 Hook 会运行。
4. 将下面这段作为新任务的首条显式锚定指令发送；这段文字应保留在锚点中，附件仅作详细背景，不应成为每次压缩都重新注入的大文档。

```text
$task-anchor 继续验证 Task Anchor 的 PostCompact 任务指令注入功能。唯一验收目标：在同一个任务内，无论手动或自动压缩多少次，压缩完成后必须立即通过 PostCompact additionalContext 原样注入第一次这条任务指令，不依赖我发送下一条消息。不得用 AGENTS.md、SessionStart、PreCompact、compact_prompt、每轮读取大文档、摘要递归或新建任务替代。必须在桌面端通过 SSH local 使用自定义 Codex CLI 0.144.4 的运行环境验证。先检查插件 Hook 是否允许/信任、当前代码和审计日志；再以可观察证据验证 UserPromptSubmit 保存与 PostCompact 恢复，并如实报告实际限制。不要排查或修改无关项目；不要继续尝试 Hand off，除非我明确要求。
```

## 六、验证时必须给出的证据

接手会话不能只说“理论上可行”，至少应给出：

1. Hook 允许/信任状态的实际证据。
2. 显式 `$task-anchor` 后的 `activation_saved` 审计事件。
3. 触发一次手动或自动压缩后，在**未依赖下一条普通用户消息**的前提下出现：

   ```text
   post_compact_received → anchor_loaded → restore_emitted
   ```

4. 恢复输出中的 `additionalContext` 确实包含同一份原始锚定指令，而不是摘要或改写版本。
5. 若无法证明运行时注入已被 Codex 主模型接收，必须明确区分：
   - 插件脚本单元测试通过；
   - Hook 实际被宿主调用；
   - 宿主实际接收 `additionalContext`；
   - 模型在压缩后继续执行时实际遵守该注入。

不要把其中任一层的成功夸大成全部成功。

## 七、接手时的边界

- 不创建另一个“记忆系统”、数据库、MCP、小模型或子代理。
- 不把所有中途用户消息都复制进锚点；当前保证对象是第一次显式 `$task-anchor` 指令。
- 不修改 `AGENTS.md` 来冒充压缩后注入方案。
- 不把 CLI 独立测试当作桌面端远程运行时验证的替代品。
- 不泄露或复制 SSH、日志、会话文件中可能出现的凭据或私人内容。
- 用户需要的是可验证的长期任务硬约束传递，不是一次性的文档总结。
